services:
  postgres:
    image: postgres
    container_name: expencfy_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: expencfy_dev
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
